Terraform foundation placeholder.

The implementation plan calls for VPC, private subnets, RDS PostgreSQL, ECS Fargate, API Gateway, EventBridge, SQS/DLQ, IAM, Secrets Manager, KMS, and CloudWatch. Add environment-specific Terraform only after the local service contracts and database migrations are stable.
